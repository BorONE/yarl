services:
  frontend:
    build: frontend
    ports:
      - "8000:80"
  envoy:
    image: envoyproxy/envoy:v1.32.2
    volumes:
      - ./envoy.yaml:/etc/envoy/envoy.yaml
    ports:
      - "8080:8080"
    extra_hosts:
      - "host.docker.internal:host-gateway"
