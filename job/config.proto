syntax = "proto3";

option go_package = "pipegraph/job";

package job;

message Config {
    oneof Job {
        ShellScriptConfig ShellScript = 1;
    }
}

message ShellScriptConfig {
    optional string Path = 1;
}
