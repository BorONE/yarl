syntax = "proto2";

package register;
option go_package = "pipegraph/register";

message ShellScriptConfig {
    required string Path = 1;
    repeated string Args = 2;
}

message ShellCommandConfig {
    optional string Command = 1;
}
