syntax = "proto2";

package register;
option go_package = "yarl/daemon";

message DaemonConfig {
    optional string Run = 1;
    optional string Status = 2;
    optional string Shutdown = 3;
}

message DaemonMonitorConfig {
}
